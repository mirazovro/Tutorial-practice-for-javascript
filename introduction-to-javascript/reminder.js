var mangoes = 16;

var person = 3;

// (/ this means vagfol)
// (% this means vagses) (% atake modulous bole arki)

// console.log(mangoes / person);
var reminder = (mangoes % person);
console.log(reminder);
